# Optional Healthchecks.io or equivalent private endpoint. Keep the real value in
# .monitor.env on the VPS (chmod 600), never in Git.
# HEALTHCHECK_PING_URL=https://hc-ping.com/replace-with-your-private-uuid
