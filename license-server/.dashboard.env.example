# Private Caddy credentials for the CatCode owner dashboard. Never commit the
# real .dashboard.env file. Generate the bcrypt hash with the command in README.
ADMIN_DASHBOARD_USERNAME=catcode-owner
ADMIN_DASHBOARD_PASSWORD_HASH=replace-with-a-caddy-bcrypt-hash
