{
  "name": "catcode",
  "private": true,
  "productName": "CatCode",
  "version": "0.2.0",
  "description": "Desktop cat companion for focus, reminders, and healthy work habits.",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "check": "node --check main.js && node --check preload.js && node --check license-client.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "electron-updater": "^6.8.3",
    "ffmpeg-static": "^5.3.0",
    "uiohook-napi": "^1.5.5"
  }
}
